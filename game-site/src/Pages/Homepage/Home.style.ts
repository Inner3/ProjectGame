import { SxProps, Theme } from '@mui/material/styles';

export const NewsStyle: SxProps<Theme> = {
    
    background: "white",
    boxShadow:1,
    display: 'flex',
    justifyContent: 'center',
    height: "auto",
    width:'auto',
    borderRadius: 3,
    border:2,
    
    
}

export const ClipsStyle: SxProps<Theme> = {
    fontSize: 24,
    fontWeight: 600,
}
